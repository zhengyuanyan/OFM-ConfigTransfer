### Format（格式）

选择输出的形式。
它不影响导出的内容，
但决定了可采用何种后续处理方式。


#### 标准（用于导入与技术支持）

导出时不加换行，全部位于一行。
若要在他设备导入中使用该输出，这是必需的，
因为 ETS 不支持多行输入。


#### 多行（无法导入！）

**注意：** 除非模块开发者明确要求，否则不应出于技术支持目的使用此格式。

导出时会在每个配置值的前后各插入一个换行。

如果模块定义中使用了可读的参数名，
开发者在简单模块上或许能直接识别出特定设置。


